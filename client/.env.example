# API Base URL (leave blank if backend serves the frontend statically on the same domain)
VITE_API_URL=http://localhost:5000/api

# For split deployment (e.g. Frontend on Vercel, Backend on Render):
# VITE_API_URL=https://your-api-domain.onrender.com/api
